#include <iostream>
#include <stack>
#include <utility>

using namespace std;

int main() {
	
	ios::sync_with_stdio(0);
	cin.tie(0);//?????? cin?? cout?? ???????? ??????

	stack<pair<int, int>> stk; //first: ?ех???(1???? ????) / second: ? ????
	int n;
	cin >> n;

	for (int i = 1; i <= n; i++) {
		int temp;
		cin >> temp;

		while (!stk.empty()) {
			if (stk.top().second < temp) { //?????? ????X
				stk.pop();
			}
			else { //?????? ????O
				cout << stk.top().first << " ";
				stk.push({ i, temp });
				break;
			}
		}
		if (stk.empty()) { //???? ?????? ??? ???? ???
			stk.push({ i, temp });
			cout << "0 ";
		}
	}

}